# {{PROJECT_NAME}} — Requirements

> What this project must do, broken into phases. Use this to plan; use `docs/issues.md` to track individual tickets.
> Tick items as complete. Add new items as scope emerges.

---

## Phase 0 — Bootstrap (✅ done {{TODAY}})

- [x] Agent-config layer in place ({{AGENT_CONFIG_FILES_LIST}})
- [x] `docs/agents/` triad written (OVERVIEW, CONVENTIONS, STATUS)
- [x] `CONTRIBUTING.md` defines the plan/test/commit workflow
- [x] `docs/issues.md` ticket tracker initialised
- [x] `docs/decisions/` ADR archive seeded with ADR-001
- [x] Pre-commit gate (`{{CHECK_COMMAND}}`) wired up
- [x] CI workflow mirrors the local gate
- [x] `docs/manual-testing.md` runbook initialised

---

## Phase 1 — Initial implementation

*Populate this section with the first chunk of real product work. Examples:*

- [ ] Project skeleton + minimal "hello world" end-to-end path
- [ ] First real test for the smallest meaningful unit
- [ ] First feature ticket landed via the full workflow (plan → approve → implement → test → commit)

---

## Future phases

*Add phases as scope clarifies. Each phase should be a logical milestone, not a sprint timeline.*
